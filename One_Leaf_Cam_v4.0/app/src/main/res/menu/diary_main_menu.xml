<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".DiaryPacket.DiaryMainActivity">

        <item android:id="@+id/change_item"
            android:title="change"
            android:orderInCategory="80"
            android:icon="@mipmap/ic_change"
            app:showAsAction="ifRoom" />

        <item android:id="@+id/delete_item"
            android:title="delete"
            android:orderInCategory="90"
            android:icon="@mipmap/ic_delete"
            app:showAsAction="ifRoom" />

        <item android:id="@+id/share_item"
            android:title="share"
            android:icon="@mipmap/ic_share"
            android:orderInCategory="100"
            app:showAsAction="ifRoom"/>

        <item android:id="@+id/search_title_item"
            android:title="search"
            android:icon="@mipmap/ic_search"
            android:orderInCategory="110"
            app:showAsAction="ifRoom"/>

    </menu>
